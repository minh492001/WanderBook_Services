server:
  port: 8080

# ===============================
# POSTGRES
# ===============================
spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/grand_lusso_hotel
    username: postgres
    password: admin@123
  main:
    banner-mode: "off"
  # ===============================
  # JPA / HIBERNATE
  # ===============================
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    #        enable_lazy_load_no_trans: true
    hibernate:
      ddl-auto: none
    show-sql: true
  servlet:
    multipart:
      max-file-size: 500KB
      max-request-size: 500KB
  mail:
    host: smtp.gmail.com
    port: 587
    username: "tu1den2vietlien0dau@gmail.com"
    password: "oijm hrja qfjk axex"
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

logging:
  level:
#    org.springframework.security: TRACE
    org.springframework.security: DEBUG
    com.wander_book.security: DEBUG
    root: warn           # Sets the default logging level to WARN (only warnings and errors will be shown)
    org:
      hibernate:
        SQL: DEBUG
        type:
          descriptor:
            sql:
              BasicBinder: TRACE

# ===============================
# APP PROPERTIES
# ===============================
auth:
  token:
    jwtSecret: 88fcbfa02ebe24ac11ab6eb676f8cd89033920f202865bf874d73de7e5effde3
    expirationInMils: 3600000

#springdoc:
#  api-docs:
#    enabled: true
#  swagger-ui:
#    enabled: true
